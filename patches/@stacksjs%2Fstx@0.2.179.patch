diff --git a/dist/utils.js b/dist/utils.js
index dddb554d0ac5a9f04b3227ab5ffefc68998e464c..00720a54cd51c1a2bfc18ad8719435f6226ffdfd 100644
--- a/dist/utils.js
+++ b/dist/utils.js
@@ -11,6 +11,7 @@ import { getPublicEnvDefine } from "./public-env.js";
 import { processScopedStyles } from "./style-scoping.js";
 import { findSfcTemplateBlock } from "./sfc-template.js";
 import { importOnce } from "./lazy-module.js";
+import { stashScriptElements } from "./html-masking.js";
 export {
   extractDirectiveNames,
   getPositionInfo,
@@ -564,7 +565,14 @@ ${triedPaths.map((p) => `  - ${p}`).join(`
     for (const [key, val] of Object.entries(parentContext))
       if (!key.startsWith("__") || internalKeysToPreserve.has(key))
         filteredParentContext[key] = val;
-    const { extractSlotContent } = await importOnce("stx/slots", () => import("./slots.js")), parsedSlotContent = extractSlotContent(slotContent), $slots = {};
+    const stashedCallerScripts = stashScriptElements(slotContent), callerClientScripts = [], componentSlotContent = stashedCallerScripts.output.replace(/\x00STX_SCRIPT_(\d+)\x00/g, (_match, index) => {
+      const script = stashedCallerScripts.scripts[Number(index)] || "", attrs = script.match(/^<script\b([^>]*)>/i)?.[1] || "", type = attrs.match(/\btype\s*=\s*["']([^"']+)["']/i)?.[1]?.toLowerCase(), executable = !type || ["module", "text/javascript", "application/javascript"].includes(type);
+      if (!/\bserver\b/i.test(attrs) && executable) {
+        callerClientScripts.push(script);
+        return "";
+      }
+      return script;
+    }), { extractSlotContent } = await importOnce("stx/slots", () => import("./slots.js")), parsedSlotContent = extractSlotContent(componentSlotContent), $slots = {};
     if (parsedSlotContent.defaultSlot?.trim())
       $slots.default = parsedSlotContent.defaultSlot;
     for (const [slotName, slotDef] of parsedSlotContent.namedSlots)
@@ -573,7 +581,7 @@ ${triedPaths.map((p) => `  - ${p}`).join(`
       ...filteredParentContext,
       ...props,
       props,
-      slot: slotContent,
+      slot: componentSlotContent,
       $slots,
       __processedComponents: branchComponents,
       __sections: {},
@@ -762,7 +770,9 @@ finally {
         output,
         dependencies: [...dependencies].filter((dependency) => !dependencyBaseline.has(dependency))
       });
-    return output;
+    return callerClientScripts.length > 0 ? `${output}
+${callerClientScripts.join(`
+`)}` : output;
  } catch (error) {
    return `[Error processing component: ${error instanceof Error ? error.message : String(error)}]`;
  } finally {
diff --git a/dist/component-renderer.js b/dist/component-renderer.js
index 4b3edc46a7d464017199d11b9e70ae6f7e423e1a..f7cb3d1ffcd16922bfb539fcf6c8e06caad97429 100644
--- a/dist/component-renderer.js
+++ b/dist/component-renderer.js
@@ -653,7 +653,7 @@ async function processCustomElementTags(
       const mergedProps = {};
       for (const [key, value] of Object.entries(resolvedProps.static))
         mergedProps[key] = value;
       for (const [key, value] of Object.entries(resolvedProps.serverDynamic))
         mergedProps[key] = value;
-      let finalContent = await renderComponentWithSlot(componentPath, mergedProps, tag.content, componentsDir, context, filePath, options, processedComponents, dependencies);
+      let finalContent = await renderComponentWithSlot(componentPath, mergedProps, restoreStashedScripts(tag.content, stashed.scripts), componentsDir, context, filePath, options, processedComponents, dependencies);
       if (Object.keys(resolvedProps.clientReactive).length > 0)
