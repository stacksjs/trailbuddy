<script server>
  const modelValue = props.modelValue || ''
  const timeRange = props.timeRange || 'day' // 'day' | 'week' | 'month' | 'year'
</script>
<div class="px-4 py-2 bg-white dark:bg-blue-gray-700 border-b border-gray-200 dark:border-blue-gray-600">
    <div class="flex items-center">
      <!-- Search bar -->
      <div class="flex-1 max-w-2xl">
        <div class="relative">
          <div class="flex absolute inset-y-0 left-0 items-center pl-3 pointer-events-none">
            <i class="text-gray-400 text-lg i-hugeicons-search"></i>
          </div>
          <input
            type="text"
            name="search"
            value="{{ modelValue }}"
            placeholder="Search emails..."
            class="block placeholder-gray-500 dark:placeholder-gray-400 pl-10 pr-3 py-2 w-full text-gray-900 sm:text-sm dark:text-gray-100 bg-gray-50 dark:bg-blue-gray-600 border border-gray-300 rounded-md dark:border-gray-600 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          >
        </div>
      </div>

      <!-- Activity chart toggle -->
      <div class="ml-4">
        <select
          name="timeRange"
          class="pl-3 pr-8 py-1.5 h-9 text-gray-900 text-sm dark:text-gray-100 bg-gray-50 dark:bg-blue-gray-600 border-0 ring-1 ring-gray-300 ring-inset rounded-md dark:ring-gray-600 focus:ring-2 focus:ring-blue-600"
        >
          <option value="day" {{ timeRange == 'day' ? 'selected' : '' }}>Last 24 Hours</option>
          <option value="week" {{ timeRange == 'week' ? 'selected' : '' }}>Last 7 Days</option>
          <option value="month" {{ timeRange == 'month' ? 'selected' : '' }}>Last 30 Days</option>
          <option value="year" {{ timeRange == 'year' ? 'selected' : '' }}>Last Year</option>
        </select>
      </div>
    </div>
  </div>
