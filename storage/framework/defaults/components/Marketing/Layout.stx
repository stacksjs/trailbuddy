<script server>
const inter = props.inter || {
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
}

const lexend = props.lexend || {
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-lexend',
}
</script>
<html
  lang="en"
  class="h-full antialiased bg-white scroll-smooth {{ {{ }} }} inter.variable lexend.variable"
>
  <body class="flex flex-col h-full">
    <slot />
  </body>
</html>
