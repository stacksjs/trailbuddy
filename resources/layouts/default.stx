<script server>
const stylesheetPath = '/assets/styles/main.css'
const scriptPath = '/assets/scripts/main.ts'
</script>

@stack('head')

<link rel="stylesheet" href="{{ stylesheetPath }}">

@yield('login-screens')

<div class="desktop">
  <div class="desktop-icons">
    @yield('desktop-icons')
  </div>

  @yield('sponsor-icon')
  @yield('trash-icon')
  @yield('windows')

  @yield('taskbar')
</div>

@yield('context-menus')
@yield('notification-popup')

<main>
  @yield('content')
</main>

<script type="module" src="{{ scriptPath }}"></script>
@stack('scripts')
