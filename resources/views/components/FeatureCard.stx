<script server>
import { defineProps, withDefaults } from 'stx'

interface FeatureCardProps {
  title?: string
  description?: string
  bgColor?: string
}

const { title, description, bgColor } = withDefaults(defineProps<FeatureCardProps>(), {
  title: 'Feature',
  description: 'Description',
  bgColor: 'bg-emerald-500/20'
})
</script>

<template>
  <div class="p-8 bg-gray-800 hover:bg-gray-750 rounded-2xl transition">
    <div class="flex items-center justify-center mb-6 h-14 w-14 rounded-xl {{ }} bgColor">
      <slot name="icon" />
    </div>
    <h3 class="mb-3 font-bold text-white text-xl">{{ title }}</h3>
    <p class="text-gray-400">{{ description }}</p>
  </div>
</template>
