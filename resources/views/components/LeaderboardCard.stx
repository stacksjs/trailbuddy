<script server>
import { defineProps, withDefaults } from 'stx'

interface LeaderboardCardProps {
  rank?: number
}

const { rank } = withDefaults(defineProps<LeaderboardCardProps>(), {
  rank: 47
})
</script>

<template>
  <div class="p-6 text-white bg-gradient-to-br from-emerald-600 to-emerald-700 rounded-2xl">
    <h3 class="flex items-center mb-4 font-bold text-lg">
      <Icon name="award" size="20" class="mr-2" />
      Weekly Leaderboard
    </h3>
    <p class="mb-4 text-emerald-100 text-sm">You're ranked #{{ rank }} this week</p>
    <button class="py-2.5 w-full font-semibold text-emerald-700 bg-white hover:bg-emerald-50 rounded-xl transition">
      View Full Leaderboard
    </button>
  </div>
</template>
